<h1>Artist Form</h1>


<%= form_for @artist do |f| %>

  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <%= f.label :bio %><br>
  <%= f.text_area :bio %><br>
  <%= f.submit %>

<% end %>
<br>
<%= link_to "Back", artist_path(@artist) %>
